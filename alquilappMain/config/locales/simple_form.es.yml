#define simple_form es locale here
es:
  simple_form:
    "yes": 'Si'
    "no": 'No'
    required:
      text: 'requerido'
      mark: '*'
      # You can uncomment the line below if you need to overwrite the whole required html.
      # When using html, text and mark won't be used.
      # html: '<abbr title="required">*</abbr>'
    error_notification:
      default_message: "Revise los problemas debajo:"
    # Examples
    # labels:
    #   defaults:
    #     password: 'Password'
    #   user:
    #