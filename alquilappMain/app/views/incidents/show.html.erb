<!--<section class="px-4 py-5 gradient-custom" style=" border-radius: .5rem .5rem 0 0;">
  <div class="row d-flex justify-content-center align-items-center">
    <div class="col-12 col-sm-6 col-md-6 col-lg-6 col-xl-4 ">
      <div class="card">
        <div class="card-body">
          <p class="card-text"><%= render @incident %></p>
        </div>
  <div>
    <%# <%= link_to "Edit this incident", edit_incident_path(@incident) %> 
    <%= link_to "Back to incidents", :back %>
    <%# <%= button_to "Destroy this incident", @incident, method: :delete %>
  </div>
      </div>
    </div>
  </div>
</section>-->
<div class="row d-flex justify-content-center align-items-center vh-100 gradient-custom">
  <div class="col-12 col-md-8 col-lg-6 col-xl-6 col-xxl-4">
    <div class="card shadow" style="border-radius: 1rem;">
      <h2 class="d-flex justify-content-center my-2">Incidente</h2> 
      <div class="card-body p-5 m-5 text center border">
        <%= render @incident %>
      </div>
      <div class="text-center mb-2">
        <% if current_user.supervisor?%>
          <%= link_to "Volver", :back %>
        <%else%>
          <%= link_to "Volver", rental_path(Rental.find_by(user_id: current_user.id, state: "active").id) %>
        <%end%>
      </div>
    </div>
  </div>
</div>